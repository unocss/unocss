.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Remove the exclamation point when using this example - it's only committed in this example to make it easy for you to see the built package without having to build yourself
!/dist