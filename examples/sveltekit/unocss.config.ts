// UnoCSS config options can be placed here or in vite.config.ts, but make sure to at least have a blank unocss.config.ts if you are having trouble getting the UnoCSS VSCode extension to work

// import { defineConfig, extractorSvelte, presetIcons, presetUno } from 'unocss'
// import { FileSystemIconLoader } from '@iconify/utils/lib/loader/node-loaders'

// export default defineConfig({
// the UnoCSS config object seen in vite.config.ts can also be placed here
// })
