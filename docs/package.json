{
  "name": "@unocss/docs",
  "private": true,
  "scripts": {
    "dev": "esno prepare.ts && vitepress --port 3333 --open",
    "build": "esno prepare.ts && vitepress build",
    "serve": "vitepress serve"
  },
  "devDependencies": {
    "unocss": "workspace:*",
    "vitepress": "^0.21.6"
  }
}
